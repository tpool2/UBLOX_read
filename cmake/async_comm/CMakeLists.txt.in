cmake_minimum_required(VERSION 3.10)

project(async_comm-download)

include(ExternalProject)
ExternalProject_Add(async_comm
    GIT_REPOSITORY      https://github.com/dpkoch/async_comm.git
    GIT_TAG             master
    SOURCE_DIR          "${CMAKE_CURRENT_BINARY_DIR}/async_comm-src"
    BINARY_DIR          "${CMAKE_CURRENT_BINARY_DIR}/async_comm-build"
    CONFIGURE_COMMAND   ""
    BUILD_COMMAND       ""
    INSTALL_COMMAND     ""
    TEST_COMMAND        "")